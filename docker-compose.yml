services:
  cli:
    build:
      context: .
      target: cli
      dockerfile: Dockerfile
    image: "laravel-container-auto-register-php-local"
    volumes:
      - ./:/opt/apps/app
  bun:
    build:
      context: .
      target: bun
      dockerfile: Dockerfile
    image: "laravel-container-auto-register-bun-local"
    volumes:
      - ./:/opt/apps/app
